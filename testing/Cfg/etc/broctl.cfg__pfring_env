# Don't send emails or summary reports, and turn on debug output.
# Specify a non-zero pfringclusterid (so that PF_RING can be tested).
# Specify an env. variable.

sendmail=
tracesummary=
debug=1
pfringclusterid=21
env_vars=GVAR=global
