# @TEST-EXEC: bash -ex %INPUT
# @TEST-EXEC: btest-diff out

. broctl-test-cleanup

newprefix=$(broctl-test-setup broctl1.cfg networks1.cfg node2.cfg)

export BROCTL_INSTALL_PREFIX=$newprefix
export PATH=$newprefix/bin:$PATH

broctl nodes > out
