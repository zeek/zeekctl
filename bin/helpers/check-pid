#! /usr/bin/env bash
#
#  check-pid <pid> 

( ps aux | grep -v check-pid | grep -v "ps aux$" | grep -v grep | awk '{print $2}' | grep -q "^$1$" )  2>/dev/null

if [ $? = 0 ]; then
   echo 0
else
   echo 1
fi

echo ~~~




