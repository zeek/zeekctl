#! /usr/bin/env bash

child=""

. `dirname $0`/broctl-config.sh
dbroctld=${bindir}/dbroctld 
. ${brobase}/how_to_run_broctl

nohup $dbroctld 0<&- &> nohup.out &

child=$!
echo $child >.pid
echo $child
